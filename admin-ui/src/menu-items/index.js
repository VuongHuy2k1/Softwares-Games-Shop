// project import
import client from './client';
import dashboard from './dashboard';
import utilities from './game';
import user from './user';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, utilities, user, client]
};

export default menuItems;
